# Changelog

Shared log of changes during the Wix → Next.js transition, maintained for **Nicolas** (SEO / marketing / Google Ads / site changes) and the build team.

**Purpose:** track *what* changed, *when*, *where*, and *how it affects* the public site, analytics, ads, SEO, and UX — so any shift in user engagement can be attributed to a specific change.

> **The rule:** No public-facing change ships — on **Wix** or the **new site** — without an entry here **and** a heads-up to Nicolas. Record baseline metrics before mirroring a change so deltas are attributable. See [ROLLOUT.md](./ROLLOUT.md) for the full strategy.

---

## How to add an entry

Add a row to the table for the relevant month. Keep entries one line where possible; use the Notes column for links (PR, Wix page, screenshot, metric snapshot).

**Legend**

- **Where:** `Wix` (live production) · `Beta` (new stack, noindex) · `New` (new site, public after cutover) · `Both` (mirrored on Wix + new)
- **Type:** `Content` · `Layout/UX` · `SEO` · `Ads` · `Analytics` · `Feature` · `Infra`
- **Impact:** expected effect on **SEO / Ads / Analytics / UX** — note "none" explicitly when there's no public exposure (e.g. beta-only).
- **Owner:** who made the change.

---

## 2026-06

| Date | Change | Where | Type | Impact (SEO / Ads / Analytics / UX) | Owner | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-06-06 | Repo initialized; README, ROLLOUT, CHANGELOG added | Beta | Infra | None — internal, no public exposure | CSS | Project kickoff |
| 2026-06-07 | Add CLAUDE.md (commit workflow + conventions) and REQUIREMENTS.md (full scope by stage); add S&C training-video-drop feature | Beta | Infra | None — internal, no public exposure | CSS | Planning docs |
| 2026-06-08 | Site architecture decision: santasknights.org is the primary hub for both Gladiators & Letters to Santa; gladiators.nyc redirects in. README/REQUIREMENTS/ROLLOUT updated; docs moved to docs/ | Beta | Infra | None yet — informs redirect/SEO plan at cutover | CSS | Affects future 301 mapping (coordinate w/ Nicolas) |
| 2026-06-08 | Add DESIGN.md (Stitch design-md format) derived from the home mock | Beta | Infra | None — internal | CSS | Design tokens / brand spec |
| 2026-06-08 | Scaffold Next.js 16 + Tailwind v4 app; build Santa's Knights homepage (9 sections, component library, tokens wired to theme) | Beta | Feature | None — beta, noindex, not public | CSS | First app code; build passes, verified locally |
| 2026-06-08 | Add Vercel beta deploy link to README.md and CLAUDE.md | Beta | Infra | None — internal doc link | CSS | https://santas-knights.vercel.app/ |
| 2026-06-09 | Architecture deepening: complete design-token seam (spacing/elevation/type scale in @theme); add SectionHeading, Card, Arrow modules; refactor sections to use them | Beta | Infra | None — internal refactor, homepage renders unchanged | CSS | No visual regression; build + lint pass |
| 2026-06-09 | Add real imagery: organize asset-library/ (press, armory, icons, site-files); place hero, Gladiators pillar/teaser photos + 7 press logos on homepage; add Photo component | Beta | Content | None — beta only; replaces homepage placeholders with real photos | CSS | Pillar 2 + Letters stay placeholders (no matching photo) |
| 2026-06-10 | Build About, Contact, and Get Involved pages (real routes); add PageHero + ContactForm components + founder headshot; populate content/site.ts with researched org/founder details (Damion DiGrazia bio, mission, programs, volunteer roles, FAQ); repoint nav/footer to real routes; correct email to contact@santasknights.org; embed Google Map on Contact | Beta | Feature | None — beta, noindex, not public | CSS | Details researched from santasknights.org, gladiators.nyc, CBS NY, Gothamist; build + interaction verified locally |
| 2026-06-10 | Reposition the site around Santa's Letters and overview: expand the Letters homepage section into a how-it-works + Operation Santa origin (founder Damion did USPS Operation Santa as a kid); add Operation Santa to the About story/founder; reframe training (Gladiators NYC) as a separate site throughout; reorder homepage to lead with Letters; rewrite copy across all pages + nav/footer in a plain human voice | Beta | Content | None — beta, noindex, not public | CSS | Per owner direction (separate training site coming); Operation Santa facts verified via USPS; build + console verified locally |
| 2026-06-12 | Wire the Supabase backend: Letters to Santa flow (submit → moderate → swipe-to-give → fulfill), admin dashboard + auth gate, donate/sponsors/links pages, engagement + letter server actions, and the RLS schema migration (0001_init); adopt Supabase's new publishable/secret API keys (`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` / `SUPABASE_SECRET_KEY`, replacing the legacy anon/service_role env names) | Beta | Feature | None — beta, noindex, not public; backend stays degraded until env vars are set | CSS | Build + typecheck pass locally; schema applied to Supabase separately |
| 2026-06-15 | Make CLAUDE.md the DB-schema source of truth (full schema + repo-layout/current-state notes) and delete `supabase/migrations/0001_init.sql`; rewire its references in README, `.env.example`, admin UI notice, and SETUP-TODO; add Playwright + `e2e/` Letters-flow scripts (outputs gitignored); remove obsolete `_stage1-design-brief.md` | Beta | Infra | None — internal, no public exposure | CSS | Schema now in CLAUDE.md → Database schema; typecheck passes |
| 2026-06-19 | Port the approved poster design across the full beta site; add account, gallery, membership, training, and online routes; reskin Letters and admin flows; reorganize source assets; rewrite public copy in a direct editorial voice; add legacy Supabase gift-link compatibility | Beta | Layout/UX | None, beta remains noindex; UX: consistent visual system, clearer copy, responsive routes, and verified Letters workflow | CSS | Lint, typecheck, production build, route smoke test, desktop/mobile review, and full Supabase E2E passed |
| 2026-06-21 | Simplify the site header by removing the brand tagline and duplicate Donate navigation link | Beta | Layout/UX | None, beta remains noindex; UX: less crowded header with one clear Donate action | CSS | Lint passed |
| 2026-06-21 | Rename the Santa's Knights navbar dropdown to About without changing its links | Beta | Content | None, beta remains noindex; UX: clearer navigation label | CSS | Lint passed |
| 2026-06-21 | Reverse the site-split decision in all docs: the whole nonprofit — including the **full Gladiators free program (content + training tracker)** — now lives on santasknights.org; only the commercial Shop + Armory stay on gladiators.nyc. Updated CLAUDE/README/plan-v2/REQUIREMENTS/GLADIATORS-SITE/ROLLOUT/SETUP-TODO/DESIGN/REDESIGN-PORT-PLAN; added docs/EXECUTION-PLAN.md (phased build plan) | Beta | Infra | None — internal docs, no public exposure; informs future architecture, SEO consolidation, and 301/redirect plan at cutover | CSS | Docs-only; app code unchanged. Affects future cross-link/redirect mapping (coordinate w/ Nicolas) |
| 2026-06-21 | Execution-Plan Phase 0 (consolidation): bring the Gladiators NYC program on-site in code. Rename the nav dropdown Classes → Gladiators NYC; replace the single outbound training link with internal per-class pages at /training/[slug] (6 classes); flip all "booking lives on a separate training site" copy (home, /training, /online, /about, /account, /contact, footer) to on-site with booking "coming soon"; reserve SHOP_HREF/ARMORY_HREF for the gladiators.nyc commercial Shop/Armory only; add sitewide Organization (NGO) JSON-LD + per-class Course schema stubs (price $0) | Beta | SEO | None now — beta stays noindex; at cutover: consolidates training authority onto the Ad-Grant domain, adds 6 indexable per-class URLs + Organization/Course structured data, removes external-site handoff | CSS | Lint + typecheck + production build pass; runtime smoke test (nav label, JSON-LD present, /training/[slug] 200, bad slug 404, no external-site copy in served HTML). Booking backend lands in Phase 5 |
| 2026-06-25 | Account model + Letters tracking: **single email+password account** for every role (drop magic-link); gate letter submission behind an account and link letters to the guardian (`guardian_user_id`); add /account "My letters" dashboard via the `my_letters` view | Beta | Feature | None — beta stays noindex, not public | CSS | E2E (DB/RLS + full-stack UI) pass; superseded the magic-link draft in ACCOUNT-MODEL.md |
| 2026-06-26 | Build the **full Gladiators training tracker** — class booking with atomic capacity enforcement, one-time liability waiver + media-consent records, instructor check-in, admin-editable XP/levels/badges, participant dashboard, members-only S&C video library, training admin + **grant CSV export**; wire **/gallery, /sponsors, /donate** to Supabase (gallery_media / partners / donations) with admin CRUD + role management; apply the training schema to beta (family_members, classes, registrations, checkins, waivers, media_consents, xp_config/xp_events, levels, badges, training_videos, private buckets, capacity trigger); update ACCOUNT-MODEL.md + CLAUDE.md schema | Beta | Feature | None — beta stays noindex, not public | CSS | tsc + lint + prod build green; **42/42 E2E pass** (training-tracker 17, account-letters 15, account-submit-ui 10) vs local prod build + real beta DB; security-review clean; principal-review findings fixed (role-demotion, atomic capacity, RLS/migration completeness, admin-lockout guard). Waiver text is placeholder pending Damion |
| 2026-06-26 | Gate **adopt-a-letter** behind a free account (reverses the guest-first donor model): `/letters/give` shows a sign-in card to logged-out visitors; reconcile the now-stale "no account needed" copy on the submit/letters/account pages and in ACCOUNT-MODEL.md | Beta | Layout/UX | None — beta stays noindex, not public | CSS | tsc + lint + build green; logged-out gate + logged-in deck verified live |
| 2026-06-26 | **Unified login + gift claim/track.** One sign-in form for everyone (`/account/login`), role decides the landing (admin→/admin, member→/account); retire /admin/login. Adopting now **records a claim** in Supabase (new `claimed` letter state + `claimed_at` + `my_gifts` view): "Gift this" claims a letter atomically, drops it from the public pool, with a self-dealing guard; members get action cards + "Gifts I'm sending"; admins get **Gifts** (pipeline) + **Signups** (rosters) monitors. Adopt-gate button copy → "Log in to gift a kid". | Beta | Feature | None — beta stays noindex, not public | CSS | tsc + lint + prod build green; **53/53 E2E pass** (gift-and-login 11, training-tracker 17, account-submit-ui 10, account-letters 15) vs local prod build + real beta DB. Provisioned beta accounts: 1 admin (Damion) + 3 member test accounts |
| 2026-06-26 | Add a header auth button (top-left): **Log In** when signed out (→ unified login) or **Dashboard** when signed in (→ /admin for admins, /account for members). Root layout resolves the user so the label/redirect is correct server-side (no flash); on mobile the Donate/Adopt CTAs move into the slide-out menu to fit | Beta | Layout/UX | None — beta stays noindex, not public; UX: one-tap access to login/dashboard from every page | CSS | tsc + lint + prod build green; header redirects verified for logged-out/member/admin (6/6) + desktop & mobile screenshots. Root layout now renders dynamically (reads auth) |
| 2026-06-27 | Move the auth button to the **top-right** as the primary header CTA (replaces the standalone "Adopt a letter" button and removes the top-left one): **Log In** signed out → unified login, **Dashboard** signed in → /admin (admins) or /account (members). "Adopt a letter" now lives in the Letters-to-Santa nav dropdown + mobile menu | Beta | Layout/UX | None — beta stays noindex, not public; UX: login/dashboard is the prominent header action | CSS | tsc + lint + prod build green; redirects verified logged-out/member/admin (6/6) + desktop & mobile screenshots |
| 2026-06-27 | Home + training refresh: new hero image + headline ("Strengthening kids and lifting **communities**"), drop Harlem/December from hero & org tagline; swap the two homepage pillar images; add the new letters painting behind "Answering kids' letters". Merge **/online into /training** — delete the /online route, add a dark "Train on your own time" + members-only video library section below Upcoming sessions; remove the Adopt/Donate band from the training page. Remove "All our links" from the footer; collapse the Gladiators NYC nav to a single /training link | Beta | Content | None — beta stays noindex, not public; UX: clearer home message, one training hub, fewer dead links | CSS | tsc + lint + prod build green; /online → 404 (all links repointed); video library verified for members; 53/53 E2E pass (no regressions) |
| 2026-06-27 | Homepage pillars: remove the serif subtitles ("It's the reason the nonprofit exists." / "Beginners welcome. No cost, ever."), darken the image overlays for legible text, and reframe the Santa's Letters copy from "around Harlem" to "children **around the world**" (pillar, intro, FAQ) | Beta | Content | None — beta stays noindex, not public; UX: more legible pillar text, broader letters framing | CSS | tsc + lint + prod build green; pillars screenshot verified |
| 2026-06-27 | Make the account dashboard's Donate button filled black/white by default (outline on hover) via a new `ghostInverse` Button variant — the ghost variant with normal/hover swapped | Beta | Layout/UX | None — beta stays noindex, not public; UX: consistent filled CTAs on the dashboard | CSS | tsc + lint + prod build green; screenshot verified |
| 2026-06-27 | Donate & Membership polish: reword the $100/$250/$500 membership tier descriptions (foam-sword-and-present framing; corporate = "for companies that would like to sponsor"); drop the duplicate `↗` glyph from membership tier buttons (animated arrow stays); on /donate, restyle the "More ways to help" box buttons (Membership tiers, Sponsorship, give links) to the filled `ghostInverse` style, delete the "Something else" box, and move the "Payment is handled…" line into a right-aligned note beside the heading | Beta | Layout/UX | None — beta stays noindex, not public; UX: clearer tier copy + consistent filled CTAs on /donate | CSS | tsc --noEmit passes |
| 2026-06-27 | /gallery → full-bleed photo wall: render all 39 photos from asset-library/photography/gallery (copied into public/images/gallery/, listed in content/galleryPhotos.ts) as a gapless CSS-columns masonry (4 cols desktop / 3 / 2 mobile), whole and unedited, with no captions, no category-filter chips, and no visible section background. Drop the Supabase `gallery_media` feed + the `GalleryGrid` filter/lightbox from this page (now statically rendered) | Beta | Layout/UX | None — beta stays noindex, not public; UX: denser edge-to-edge photo wall. Note: admin-managed `gallery_media` rows no longer surface on /gallery | CSS | tsc passes; desktop + mobile screenshots verified; +~13 MB imagery, lazy-loaded |
| 2026-06-27 | Admin dashboard: replace the **Roles** tab with a **Users** roster (`/admin/users`). Collapsed rows now show name / email / role (role as plain colored text, no pills); an expand arrow per row reveals that person's gifts submitted, gifts fulfilled, classes attended, XP, joined date, and waiver, plus an inline role/status editor (transparent outlined dropdown + green square Save). Display name is derived from the person's letters/waivers since `profiles` stores none. Remove the **XP & Levels**, **Gallery**, and **Partners** admin tabs + pages (no longer changeable here). Repo hygiene: gitignore + untrack the `dogfood-output/` QA artifacts | Beta | Feature | None — admin-only, beta stays noindex, not public | CSS | tsc + prod build green; admin login + /admin/users collapsed & expanded verified via screenshot |
| 2026-06-27 | Admin polish: replace the pill tab nav with a square editorial underline tab bar (per DESIGN.md — pills are for status chips only); on the Users roster show the waiver as "Signed" (green) / "Not signed" (red) and drop the redundant "Set a role by email" box (roles still edit inline per row). Make the /gallery photo wall's ground red (matches the CTA band below) instead of brown | Beta | Layout/UX | None — beta stays noindex, not public; UX: cleaner admin nav, clearer waiver status, more cohesive gallery | CSS | tsc + lint + prod build green; admin/users/gallery screenshots verified |
| 2026-06-27 | Admin: red-outline sign-out on the title row + stop the tab bar from scrolling vertically. Darken low-contrast body text on the About story cards and Sponsors tiers. Trim the About nav dropdown to **About Us + Sponsors** (drop Founder/Members/Partners), remove the partners section from /about, and add hero images to the About and Adopt-a-letter pages (subtitle moved to the right column) | Beta | Layout/UX | None — beta stays noindex, not public; UX: more legible text, simpler nav, richer page heroes | CSS | tsc + lint + prod build green; screenshots verified |
| 2026-06-27 | Merge the Letters flows onto one page: `/letters/give` now hosts both **Adopt a letter** and **Submit your child's letter** behind an animated segmented toggle (new client `LettersPortal`) that crossfades between the two sides with no route change; always defaults to Adopt, with `?do=submit` deep-linking to the form. `/letters/submit` now redirects in; `links.submitLetter` + nav/footer repointed. Swap the Adopt hero photo to DSC_5880 and top-align `PageHero` media with the title. Class cards: drop the audience/duration pills for a `·`-separated meta line (training page + homepage). Waiver page: remove the page title, move the full-contact intro full-width above the box, and only show + require the parent/guardian field once the entered date of birth is under 18 (new client `WaiverForm`) | Beta | Feature | None — beta stays noindex, not public; UX: one Letters destination with a smooth Adopt/Submit toggle, cleaner class cards, simpler age-gated waiver | CSS | tsc + lint + prod build green; toggle (incl. mid-transition), class cards, and waiver page verified via screenshot |
| 2026-06-27 | Letters toggle: stop the page from jumping on switch — stack both hero captions in one grid cell so the header reserves the taller side's height and never reflows (body still crossfades). Collapse the **Letters to Santa** nav dropdown (Adopt/Write) to a single link → `/letters`. Rewrite the About founder bio to owner-supplied copy (founding year confirmed 2015; service-disabled-veteran 501(c)(3) framing, Operation Santa + line-of-duty-injury origin, Wall Street/Ivy League background, welcome close) | Beta | Layout/UX | None — beta stays noindex, not public; UX: no toggle jump, simpler nav, updated founder story | CSS | tsc + lint green; toggle height stability (tablist top + hero height identical pre/post switch), nav link, and founder copy verified via screenshot |
| 2026-06-27 | Remove the founder pull-quote block from **/about** (the founder section now ends after the bio); correct the now-unused `founder.quoteAttribution` from "CBS New York" to **Gothamist** — the quote's verified source is the Gothamist feature, not the CBS article | Beta | Content | None — beta stays noindex, not public; UX: tighter founder section, accurate source credit | CSS | tsc passes |
| 2026-06-27 | Fix the homepage "Seen in" press strip: the old `grayscale+brightness(0)+invert` filter only worked on transparent PNGs, so the opaque-JPEG logos rendered as flat gray blocks (only The Guardian showed). Now each logo sits on a white chip with `mix-blend-multiply` (renders any format) and **links to its real article** (opens new tab). Curated to verified, live coverage only (all 200): Gothamist (new), ABC News/Nightline, Business Insider, Men's Journal, Yahoo, Gizmodo. Dropped **The Guardian** and **New York Magazine** — no traceable article exists for either (neither is on Gladiators NYC's own press page), so the logos couldn't back a coverage claim; their image files remain unused in `public/images/press/` | Beta | Content | None — beta stays noindex, not public; UX: press logos actually render + are clickable; credibility: only real, linked coverage shown | CSS | tsc + lint + prod build green; all 6 logos load + link verified via screenshot; article URLs checked 200 |
| 2026-06-27 | Press strip: swap the ABC News and Business Insider logos for clean transparent-PNG versions (owner-supplied) — ABC now renders as the proper black circle mark and BI as a sharp wordmark, replacing the old low-res/dark JPEGs. Deleted `abc-news.jpg` + `business-insider.jpg`, added `abc-news.png` + `business-insider.png`, repointed `pressLogos` src | Beta | Content | None — beta stays noindex, not public; UX: crisper, on-brand press logos | CSS | tsc + prod build green; all 6 logos render verified via screenshot |
| 2026-06-27 | Letters route consolidation: move the combined Adopt/Submit experience to a single canonical `/letters` route; `/letters/give` and `/letters/submit` now `permanentRedirect` (308) to `/letters` (preserving the `?do=submit` deep-link). Repoint `links.adoptLetter`/`links.submitLetter`, the SwipeDeck login `next`, and all `revalidatePath` calls (admin + give actions) to `/letters`; the toggle keeps the URL constant at `/letters` | Beta | Layout/UX | None — beta stays noindex, not public; UX: one canonical Letters URL; old links 308-redirect in. Note: affects future 301/redirect mapping at cutover (coordinate w/ Nicolas) | CSS | tsc + lint + prod build green |
| 2026-06-27 | Sponsors: add real transparent-PNG logos (sourced from Wikimedia) for Google, Graham Windham, Whole Foods Market, NYU, Kohl's, Wounded Warrior Project, and NYPD Community Affairs into `public/images/sponsors/`, rendered in the `/sponsors` tiles (the other 8 orgs keep text-tile fallback — no clean logo found). Cut the homepage "In good company" strip to a featured subset (those 6 names) via a new `featured` flag, keeping the wordmark design, and add a "See all sponsors →" button aligned with the heading → `/sponsors`. Make `/sponsors` read the single static `sponsors` array (drop the orphaned `getPartners` DB path + `force-dynamic`) | Beta | Content | None — beta stays noindex, not public; UX: recognizable sponsor logos on /sponsors, tighter homepage strip with a path to the full list | CSS | tsc + lint + prod build green; 7 logos load + homepage alignment verified via screenshot |
| 2026-06-27 | Merge the Letters program onto one page: the adopt/submit portal now lives at **`/letters`** (was `/letters/give`), and toggling Adopt⇄Submit no longer changes the URL — one canonical link, with `?do=submit` only deep-linking the initial side on load. Move the "How we protect the kids" privacy/safety section onto `/letters` (kept for visitors + SEO); the old `/letters` about page (how-it-works, two-doors, origin, closing CTA) is retired as redundant. 308-redirect `/letters/give`, `/letters/give?do=submit`, and `/letters/submit` to the new URLs; repoint every inbound link (nav, footer, CTAs, login `next`, pool revalidation), and drop the dead `lettersLearnMore` link | Beta | Layout/UX | None — beta stays noindex, not public; UX: one Letters URL that's both info and adopt/submit, stable on toggle; at cutover consolidates the program onto a single indexable page (old URLs 308 in) | CSS | tsc passes; /letters 200, `/letters/give`(+`?do=submit`) and `/letters/submit` 308 → new URLs, toggle keeps URL constant, `?do=submit` deep-link + adopt/submit/privacy sections verified via screenshot; redirects also preserve `?demo=1`; account-submit E2E repointed to the new URL |
| 2026-06-27 | Letters route follow-ups: add short permanent-redirect aliases **`/letters/g` → `/letters`** and **`/letters/s` → `/letters?do=submit`**; repoint the remaining `/letters/give`·`/letters/submit` references (submit consent `submitted_via` label, ACCOUNT-MODEL + SETUP-TODO docs) and update the `e2e/` flows (account-gated sign-in before submit; donor deck now at `/letters`) | Beta | Infra | None — internal docs/tests + beta-only redirect aliases, no public exposure | CSS | tsc passes |
| 2026-06-27 | /sponsors logo tiles: un-grayscale/color the logo on hovering the **whole tile** (`group` + `group-hover`), not just the logo image | Beta | Layout/UX | None — beta stays noindex, not public; UX: larger, more forgiving hover target on sponsor logos | CSS | tsc passes; box-edge hover verified via screenshot |
| 2026-06-27 | Fix faint section titles: headings on light sections that omitted an explicit color were inheriting the light `bone` body default and rendering as faint grey. Add `text-ink` to the affected card/section titles on **/membership, /donate, /contact, /get-involved, /training/waiver, and /account** | Beta | Layout/UX | None — beta stays noindex, not public; UX: readable (dark) section titles across these pages | CSS | tsc passes; membership/donate/contact/get-involved titles verified dark via screenshot |
| 2026-06-27 | /account layout: move the **Training/classes dashboard above** "Gifts I'm sending", and put **"Gifts I'm sending" and "My letters" side by side** (`lg:grid-cols-2`). Refactor `MyGifts` from its own section into a panel; cards in each half-column stack 1-up on desktop, 2-up when the panels stack on smaller screens | Beta | Layout/UX | None — beta stays noindex, not public; UX: training surfaced higher, gifts/letters tracking side by side | CSS | tsc passes; side-by-side layout verified via screenshot |
| 2026-06-27 | **Letters: drop the moderation queue.** Submissions now go **live immediately** (status `live`) and admins **delete** problem letters instead of approving them. Simplify `letter_status` to `live`/`fulfilled`/`deleted`; rework the `public_letters` (live + unclaimed), `my_letters` (drops `moderation_note`), and `my_gifts` views; update the submit action, the admin Letters/Gifts dashboards, account status labels, and the Letters-page copy ("You submit, it goes live"); bump guardian consent to `draft-3` ("may remove any submission if needed"). Update CLAUDE.md schema + ACCOUNT-MODEL/SETUP-TODO + e2e; add `docs/sql/2026-06-letters-live-deleted.sql`. **Requires applying that SQL to Supabase.** | Beta | Feature | None — beta stays noindex, not public; behavior: letters go live instantly, lighter admin (delete vs approve) | CSS | tsc + prod build green; **DB migration must be applied separately to Supabase** |
| _e.g._ 2026-06-XX | _Consolidate social/stream links on Links page_ | Both | Content | _SEO: neutral · Ads: none · Analytics: track click-through delta · UX: fewer dead links_ | Nicolas | _Baseline recorded first; mirror on Wix, then build on new site_ |

## 2026-07

| Date | Change | Where | Type | Impact (SEO / Ads / Analytics / UX) | Owner | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-03 | Docs: reflect the Plan v2 split — the Gladiators *operational* training tracker (booking, waivers, check-in, XP, dashboards, videos, admin) plus Shop/Armory now build on the separate `gladiators.nyc` site; this repo keeps the Gladiators *content* pages only. Reword CLAUDE.md + README.md to match; relocate docs from `docs/` to the repo root | Beta | Infra | None — internal docs, no public exposure | CSS | Companion `gladiators.nyc` repo scaffolded + planned separately; no app code changed in this repo |
| 2026-07-11 | Founder-quote section (`GladiatorsTeaser`, home `#training`): removed the three training stat chips (Full-contact / Free / Since 2013) — Gladiators-specific and factually off (wrong year, unverified superlative) on a quote that spans the whole nonprofit; fixed a tight italic-to-upright gap in the pull-quote ("entirely by"); dropped the now-unused `gladiatorsMeta` data | Beta | Layout/UX | SEO: none · Ads: none · Analytics: none · UX: cleaner quote block, removes inaccurate stats — all beta/noindex | CSS | Home `#training` section |
| 2026-07-11 | Homepage motion pass: subtle scroll-reveal (fade + 18px rise) on each home section, hover-lift + red CTA glow on buttons and cards (implements the long-specced DESIGN.md "hover = lift"), gliding CTA arrow, staggered hero load-in with a settle-in photo zoom, and a count-up on the 100% impact stat. Adds a shared `Reveal` IntersectionObserver; full `prefers-reduced-motion` and no-JS fallbacks keep content visible | Beta | Layout/UX | SEO: none · Ads: none · Analytics: none · UX: warmer, more alive homepage; motion only when the OS allows it — all beta/noindex | CSS | Home; adds `components/ui/Reveal.tsx`, motion CSS in `app/globals.css` |
| 2026-07-11 | Bundled working-tree snapshot committed alongside the motion pass: in-progress edits across ~28 pages/components (about, donate, account, admin, contact, gallery, letters/SwipeDeck, links, membership, sponsors, navbar, footer, section + UI components) and `content/site.ts` / `galleryPhotos.ts`; added real press logos (LA Times, NPR, Yahoo). Removed the obsolete `design-demos/` static HTML mockups + screenshots (the live React components are the source of truth) | Beta | Layout/UX | None — beta/noindex, no public exposure | CSS | Snapshot bundled at user request; individual page intents not itemized |

<!--
Copy this row to add an entry:
| YYYY-MM-DD | <what changed> | Wix/Beta/New/Both | Content/Layout/SEO/Ads/Analytics/Feature/Infra | SEO: … · Ads: … · Analytics: … · UX: … | <owner> | <links/notes> |
-->

---

## Pre-cutover baselines

Record before the first Track B (Wix-mirrored) change, so engagement shifts are attributable. _(To be filled in with Nicolas.)_

| Metric | Baseline | Date recorded | Source |
| --- | --- | --- | --- |
| Sessions / mo | _TBD_ | | GA4 |
| Organic clicks / mo | _TBD_ | | Search Console |
| Top landing pages | _TBD_ | | GA4 |
| Active Google Ads destinations | _TBD_ | | Google Ads |
| Core Web Vitals / Lighthouse | _TBD_ | | PageSpeed / Lighthouse |
